import { Layout } from 'antd'

const { Header: AHeader } = Layout

export const Header = () => {
  return (
    <AHeader
      style={{
        background: '#fff',
      }}>
      Header
    </AHeader>
  )
}
